name: Analysestate Docker Image
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build_and_publish:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Analyse Dockerfile avec hadolint
        run: |
         docker run --rm -i hadolint/hadolint < ./super_calculator/Dockerfile

 

   

    
